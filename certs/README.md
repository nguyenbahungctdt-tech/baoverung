# Signing assets placeholder

This folder is intended for Apple signing assets used to build and export an iOS .ipa.

Place these files here:
- `development.p12` or `distribution.p12`
- `certificate-password.txt` (optional, for local reference)

For real signing, export a certificate from Keychain Access and a provisioning profile from Apple Developer.
