# Provisioning profiles placeholder

Place your real `.mobileprovision` files here, for example:
- `SampleApp.mobileprovision`

This folder is used by the build scripts and GitHub Actions workflow when signing assets are provided.
